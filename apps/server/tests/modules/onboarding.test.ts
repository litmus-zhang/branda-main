import { describe, expect, it } from "bun:test"
import * as pactum from "pactum"

describe("Onboarding", () => {
  it.todo("POST /onboarding - should onboard a member  and collect credentials", () => {})
  it.todo("GET /onboarding - should get the onboarding status", () => {})
  it.todo("PUT /onboarding - should update the onboarding and provide more credentials", () => {})
})
