import * as restate from "@restatedev/restate-sdk";
import { aiService } from "./services/ai/restate.js";

// Define the Restate endpoint and register services
// export const restateHandler = restate.endpoint().bind(aiService).handler();
